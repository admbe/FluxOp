# Generated by scripts/generate_rill_project.py -- do not edit.

version: 1
type: metrics_view
display_name: Flux commitments
model: flux_commitments

dimensions:
  - name: sku
    column: sku
  - name: resource_type
    column: resource_type
  - name: region
    column: region
  - name: term
    column: term
    description: P1Y or P3Y
  - name: scope_type
    column: scope_type
  - name: state
    column: state

measures:
  - name: reservation_count
    expression: COUNT(*)
  - name: total_quantity
    expression: SUM(quantity)
  - name: average_utilization_7d
    expression: AVG(utilization_7d)
    description: Azure-reported 7-day utilization of purchased capacity.
    format_preset: percentage
  - name: average_utilization_30d
    expression: AVG(utilization_30d)
    format_preset: percentage
  - name: underused_reservations
    expression: COUNT(CASE WHEN utilization_30d IS NOT NULL AND utilization_30d < 80 THEN 1 END)
    description: Reservations under 80% utilization over 30 days -- money already spent that workloads are not consuming.
  - name: expiring_within_90d
    expression: COUNT(CASE WHEN expiry_date IS NOT NULL AND expiry_date <= current_date + INTERVAL 90 DAY THEN 1 END)
    description: Reservations that need a renew-or-release decision soon.
