# Generated by scripts/generate_rill_project.py -- do not edit.

version: 1
type: metrics_view
display_name: Flux cost anomalies
model: flux_cost_anomalies
timeseries: evaluation_date

dimensions:
  - name: severity
    column: severity
  - name: cost_type
    column: cost_type
  - name: scope_type
    column: scope_type
  - name: service_name
    column: service_name
  - name: subscription_name
    column: subscription_name
    description: Friendly subscription name; falls back to the id
  - name: subscription_id
    column: subscription_id
  - name: resource_group
    column: resource_group

measures:
  - name: anomaly_count
    expression: COUNT(*)
  - name: total_absolute_change
    expression: SUM(absolute_change)
    description: Sum of spend movement versus baseline.
    format_preset: currency_usd
  - name: current_total
    expression: SUM(current_amount)
    format_preset: currency_usd
  - name: baseline_total
    expression: SUM(baseline_median)
    format_preset: currency_usd
  - name: max_percent_change
    expression: MAX(percent_change)
    format_preset: percentage
