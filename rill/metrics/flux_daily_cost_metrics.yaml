# Generated by scripts/generate_rill_project.py -- do not edit.

version: 1
type: metrics_view
display_name: Flux daily cost
model: flux_daily_cost
timeseries: usage_date

dimensions:
  - name: cost_type
    column: cost_type
    description: ActualCost or AmortizedCost
  - name: subscription_name
    column: subscription_name
    description: Friendly subscription name; falls back to the id
  - name: subscription_id
    column: subscription_id
  - name: service_name
    column: service_name
  - name: resource_id
    column: resource_id
  - name: currency
    column: currency
  - name: source
    column: source
    description: Collector that produced the line

measures:
  - name: total_cost
    expression: SUM(amount)
    description: Cost within one explicit cost type and currency.
    format_preset: currency_usd
  - name: average_daily_cost
    expression: SUM(amount) / NULLIF(COUNT(DISTINCT usage_date), 0)
    description: Total cost divided by the number of observed days.
    format_preset: currency_usd
  - name: distinct_resources
    expression: COUNT(DISTINCT NULLIF(resource_id, ''))
    description: Resources with at least one cost line.
  - name: cost_rows
    expression: COUNT(*)
    description: Underlying cost lines.
