# Generated by scripts/generate_rill_project.py -- do not edit.

version: 1
type: metrics_view
display_name: Flux inventory
model: flux_inventory

dimensions:
  - name: resource_type
    column: resource_type
  - name: subscription_name
    column: subscription_name
  - name: resource_group
    column: resource_group
  - name: region
    column: region
  - name: sku
    column: sku
  - name: opportunity_kind
    column: opportunity_kind

measures:
  - name: resource_count
    expression: COUNT(*)
  - name: estimated_monthly_cost
    expression: SUM(coalesce(estimated_monthly_cost, 0))
    description: Directional monthly cost from the enrichment pipeline.
    format_preset: currency_usd
  - name: estimated_monthly_savings
    expression: SUM(coalesce(estimated_monthly_savings, 0))
    description: Directional savings attached to detected opportunities.
    format_preset: currency_usd
  - name: average_utilization
    expression: AVG(utilization_percent)
    description: Mean utilization across resources with telemetry.
    format_preset: percentage
  - name: tagged_percent
    expression: 100.0 * COUNT(CASE WHEN coalesce(CAST(tags_json AS VARCHAR), '') NOT IN ('', '{}', 'null') THEN 1 END) / NULLIF(COUNT(*), 0)
    description: Share of resources carrying at least one tag.
    format_preset: percentage
